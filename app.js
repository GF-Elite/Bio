// Menggunakan event listener untuk menghentikan klik kanan pada halaman
document.addEventListener('contextmenu', function(event) {
  event.preventDefault(); // Menghentikan tindakan default menu konteks
});
